list_util
filter_by_list возвращает элементы которые есть в 1-ом списке, но нет во 2-ом. Сложность O(n^2).
Можно добится сложности стремящейся -> O(n) в худшем случае O(n^2):
1. создать словарь из списка 2 (но для этого объекты должны быть хэшируемы)
2. пробежать по каждому значению в 1 списке - O(n)
3. проверить содержится ли он в словаре - O(1) в худшем случае O(n)
4. если не содержится то добавить в результирующий массив

filter_zeros удаляет из списка нули, используя только О(1) дополнительной памяти.